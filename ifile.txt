<token_desc> token{
    <right>{ unary_minus; }
    <left>{
        div;
        minus;
    }
    identifier<std::string>;
}

<grammar> grammar{
    Expr<std::string>
        : [make_sub] Expr(0) minus Expr(1)
        | [make_div] Expr(0) div Expr(1)
        | []
        | [make_unary_minus] <unary_minus> minus Expr(0)
        | [make_term] identifier(0)
        ;
}
